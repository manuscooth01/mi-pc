name: RDP-Moonlight

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          # Habilitar Remote Desktop y desactivar NLA (para máxima compatibilidad)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force

          # Eliminar regla previa para evitar duplicados
          netsh advfirewall firewall delete rule name="RDP-Allow" 2>$null

          # Permitir RDP solo desde la red Tailscale (CGNAT 100.64.0.0/10)
          netsh advfirewall firewall add rule name="RDP-Allow" dir=in action=allow protocol=TCP localport=3389 remoteip=100.64.0.0/10

          # Reiniciar el servicio de Escritorio Remoto
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        env:
          FIXED_PASSWORD: ${{ secrets.RDP_PASSWORD }}
        run: |
          if ($env:FIXED_PASSWORD) {
              $password = $env:FIXED_PASSWORD
              Write-Host "Using fixed password from RDP_PASSWORD secret"
          } else {
              # Generación de contraseña robusta
              $charSet = @{
                  Upper   = [char[]](65..90)
                  Lower   = [char[]](97..122)
                  Number  = [char[]](48..57)
                  Special = ([char[]](33..47) + [char[]](58..64) + [char[]](91..96) + [char[]](123..126))
              }
              $rawPassword = @()
              $rawPassword += $charSet.Upper | Get-Random -Count 4
              $rawPassword += $charSet.Lower | Get-Random -Count 4
              $rawPassword += $charSet.Number | Get-Random -Count 4
              $rawPassword += $charSet.Special | Get-Random -Count 4
              $password = -join ($rawPassword | Sort-Object { Get-Random })
          }

          # Limpiar usuario previo (defensivo)
          if (Get-LocalUser -Name "RDPUser" -ErrorAction SilentlyContinue) {
              Remove-LocalUser -Name "RDPUser"
          }

          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDPUser" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDPUser"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDPUser"

          if (-not (Get-LocalUser -Name "RDPUser" -ErrorAction SilentlyContinue)) {
              Write-Error "User creation failed"
              exit 1
          }

          echo "RDP_USERNAME=RDPUser" >> $env:GITHUB_ENV
          echo "RDP_PASSWORD=$password" >> $env:GITHUB_ENV

      - name: Install Tailscale (Latest Version)
        run: |
          # Usar la URL "latest" para obtener siempre la última versión estable
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $installerPath = Join-Path $env:TEMP "tailscale-setup.msi"

          Write-Host "Downloading Tailscale..."
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath

          Write-Host "Installing Tailscale..."
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait

          $tsExe = Join-Path $env:ProgramFiles "Tailscale\tailscale.exe"
          if (-not (Test-Path $tsExe)) {
              Write-Error "Tailscale installation failed"
              exit 1
          }
          Write-Host "Tailscale installed: $tsExe"
          Remove-Item $installerPath -Force -ErrorAction SilentlyContinue

      - name: Establish Tailscale Connection
        run: |
          $tsExe = Join-Path $env:ProgramFiles "Tailscale\tailscale.exe"
          $hostname = "gh-runner-$env:GITHUB_RUN_ID"

          Write-Host "Connecting Tailscale as: $hostname"
          & $tsExe up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=$hostname --accept-routes

          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 12) {
              Start-Sleep -Seconds 5
              $tsIP = (& $tsExe ip -4 2>$null) | Select-Object -First 1
              $retries++
              Write-Host "Attempt $retries/12 - IP: $tsIP"
          }

          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned"
              & $tsExe status
              exit 1
          }

          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
          Write-Host "Tailscale IP assigned: $tsIP"

      - name: Install VB-Cable (Virtual Audio)
        run: |
          # VB-Cable es esencial para que Sunshine capture audio en un entorno sin sonido físico.
          $zip = Join-Path $env:TEMP "vbcable.zip"
          Invoke-WebRequest -Uri "https://download.vb-audio.com/Download_CABLE/VBCABLE_Driver_Pack45.zip" -OutFile $zip
          Expand-Archive $zip -DestinationPath "$env:TEMP\vbcable" -Force
          $setup = Get-ChildItem "$env:TEMP\vbcable" -Recurse -Filter "*Setup_x64.exe" | Select-Object -First 1
          if ($setup) {
              Start-Process $setup.FullName -ArgumentList "-i","-h" -Wait
              Write-Host "VB-Cable installed successfully."
          } else {
              Write-Host "WARNING: VB-Cable setup not found. Audio may not work."
          }

      - name: Install Sunshine (Latest Version)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $headers = @{ Authorization = "token $env:GH_TOKEN" }
          $installerUrl = $null
          
          try {
              Write-Host "Fetching latest Sunshine release from GitHub API..."
              $releaseInfo = Invoke-RestMethod -Uri "https://api.github.com/repos/LizardByte/Sunshine/releases/latest" -Headers $headers -ErrorAction Stop
              $installerUrl = ($releaseInfo.assets | Where-Object { $_.name -like "*WindowsInstaller.exe" }).browser_download_url
              
              if (-not $installerUrl) {
                  Write-Host "No WindowsInstaller.exe found in the latest release. Assets available:"
                  $releaseInfo.assets | ForEach-Object { Write-Host " - $($_.name)" }
              }
          } catch {
              Write-Host "API request failed or rate limited. Using fallback URL."
          }

          # Si la API falló, usamos una URL de respaldo
          if (-not $installerUrl) {
              $installerUrl = "https://github.com/LizardByte/Sunshine/releases/download/v0.23.1/sunshine-windows-installer.exe"
              Write-Host "Using fallback URL: $installerUrl"
          }

          $installer = Join-Path $env:TEMP "sunshine-installer.exe"
          Write-Host "Downloading Sunshine from: $installerUrl"
          Invoke-WebRequest -Uri $installerUrl -OutFile $installer
          Start-Process $installer -ArgumentList "/S" -Wait

          $sunshineExe = "C:\Program Files\Sunshine\sunshine.exe"
          if (-not (Test-Path $sunshineExe)) {
              Write-Error "Sunshine installation failed"
              exit 1
          }
          Write-Host "Sunshine installed."

          # Configurar credenciales de la interfaz web de Sunshine
          & $sunshineExe --creds "$env:RDP_USERNAME" "$env:RDP_PASSWORD"

          # Configurar el sink de audio de Sunshine a VB-Cable
          $sunshineConf = "C:\Program Files\Sunshine\config\sunshine.conf"
          if (Test-Path $sunshineConf) {
              Add-Content -Path $sunshineConf -Value "audio_sink = CABLE Input"
              Write-Host "Sunshine audio sink set to 'CABLE Input'."
          }

          # Firewall para los puertos de Sunshine (solo desde Tailscale)
          netsh advfirewall firewall delete rule name="Sunshine-TCP" 2>$null
          netsh advfirewall firewall delete rule name="Sunshine-UDP" 2>$null
          netsh advfirewall firewall add rule name="Sunshine-TCP" dir=in action=allow protocol=TCP localport=47984,47989,47990,48010 remoteip=100.64.0.0/10
          netsh advfirewall firewall add rule name="Sunshine-UDP" dir=in action=allow protocol=UDP localport=47998-48000 remoteip=100.64.0.0/10

          # Iniciar el servicio Sunshine
          Start-Service SunshineService -ErrorAction SilentlyContinue
          Write-Host "Sunshine service started."

      - name: Install Virtual Display Driver (IDD)
        run: |
          # Instala el driver que permite a Sunshine capturar una pantalla sin monitor físico.
          $vddZip = Join-Path $env:TEMP "vdd.zip"
          Invoke-WebRequest -Uri "https://github.com/qianwei-github/Virtual-Display-Driver/releases/download/v23.10.19/VirtualDisplayDriver.zip" -OutFile $vddZip
          Expand-Archive $vddZip -DestinationPath "$env:TEMP\vdd" -Force

          $installer = Get-ChildItem "$env:TEMP\vdd" -Recurse -Filter "*.bat" | Where-Object { $_.Name -like "*install*" } | Select-Object -First 1
          if ($installer) {
              Write-Host "Running Virtual Display Driver installer..."
              Start-Process $installer.FullName -ArgumentList "/S" -Wait -NoNewWindow
              Write-Host "Virtual Display Driver installed."
          } else {
              Write-Host "WARNING: Virtual Display Driver installer not found. Screen capture may be black."
          }

      - name: Create Console-Switch Script
        run: |
          # Mueve la sesión RDP a la consola para que Sunshine pueda capturarla.
          $bat = @'
          @echo off
          net session >nul 2>&1
          if %errorlevel% neq 0 (
              powershell -Command "Start-Process '%~f0' -Verb RunAs"
              exit /b
          )
          tscon %SESSIONNAME% /dest:console
          '@
          Set-Content -Path "C:\Users\Public\Desktop\Switch-to-Moonlight.bat" -Value $bat
          Write-Host "Desktop script created: Switch-to-Moonlight.bat"

      - name: Verify Ports
        run: |
          Write-Host "Verifying ports..."
          foreach ($port in 3389, 47990) {
              $r = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port $port -WarningAction SilentlyContinue
              Write-Host "Port ${port}: $(if ($r.TcpTestSucceeded) {'OK'} else {'FAIL'})"
          }

      - name: Maintain Connection & Auto-Restart
        env:
          GH_TOKEN: ${{ secrets.WORKFLOW_PAT }}
        run: |
          Write-Host ""
          Write-Host "========================================="
          Write-Host "         CONNECTION DETAILS"
          Write-Host "========================================="
          Write-Host "  Address:  $env:TAILSCALE_IP"
          Write-Host "  Port:     3389"
          Write-Host "  Username: $env:RDP_USERNAME"
          Write-Host "  Password: $env:RDP_PASSWORD"
          Write-Host "  Sunshine Web UI: https://$($env:TAILSCALE_IP):47990"
          Write-Host "========================================="
          Write-Host ""

          # Mantener activo 3500 min (margen antes del limite real de GitHub de 6h/360min)
          $deadline = (Get-Date).AddMinutes(3500)
          while ((Get-Date) -lt $deadline) {
              $left = [int](($deadline - (Get-Date)).TotalMinutes)
              Write-Host "[$(Get-Date -Format 'HH:mm:ss')] RDP Active - $left min remaining"
              Start-Sleep -Seconds 300
          }

          if ($env:GH_TOKEN) {
              Write-Host "Auto-restarting workflow..."
              gh workflow run "$env:GITHUB_WORKFLOW" --ref "$env:GITHUB_REF"
              Start-Sleep -Seconds 60
          } else {
              Write-Host "WARNING: WORKFLOW_PAT not configured - session ends here"
              Start-Sleep -Seconds 300
          }